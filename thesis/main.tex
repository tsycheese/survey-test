% ============================================
% 毕业设计论文主文件
% 基于 ctexart 文档类，支持中文
% ============================================
\documentclass[12pt,a4paper,fontset=windows]{ctexart}

% ---------- 页面设置 ----------
\usepackage{geometry}
\geometry{
  top=2.54cm, bottom=2.54cm,
  left=3.17cm, right=3.17cm,
  headheight=15pt
}

% ---------- 英文字体（Times New Roman） ----------
\usepackage{ifxetex}
\ifxetex
  \setmainfont{Times New Roman}
  \setmonofont{Times New Roman}
\else
  \usepackage{mathptmx}
\fi

% ---------- 基础包 ----------
\usepackage{graphicx}          % 图片插入
\usepackage{xcolor}            % 颜色
\usepackage{amsmath,amssymb}   % 数学公式
\usepackage{booktabs}          % 专业表格
\usepackage{longtable}         % 跨页表格
\usepackage{multirow}          % 合并单元格
\usepackage{array}             % 表格增强
\usepackage{enumitem}          % 列表定制
\usepackage{float}             % 浮动体控制
\usepackage{caption}           % 图表标题
\usepackage{subcaption}        % 子图标题
\usepackage[newfloat,section]{minted}   % 代码高亮（基于 pygments）
\SetupFloatingEnvironment{listing}{listname={代码清单},name={代码}}
\usepackage{hyperref}          % 超链接
\usepackage{bookmark}          % PDF 书签
\usepackage{fancyhdr}          % 页眉页脚
\usepackage{setspace}          % 行距设置
\usepackage{tocloft}           % 目录格式
\usepackage{xurl}              % 允许 URL 在任意位置断行

% ---------- 代码高亮设置 ----------
\setminted{
  fontsize=\small,
  linenos=true,
  numbersep=8pt,
  frame=single,
  breaklines=true,
  tabsize=2,
  xleftmargin=2em,
  autogobble=true
}
\usemintedstyle{friendly}  % 配色方案

% 代码块单独使用 Consolas（正文 \texttt 仍用 Times New Roman）
\newfontfamily\codefont{Consolas}
\setminted{formatcom=\codefont}

% ---------- 超链接设置 ----------
\hypersetup{
  colorlinks=true,
  linkcolor=black,
  citecolor=blue,
  urlcolor=blue,
  bookmarksnumbered=true,
  pdfborder={0 0 0}
}

% ---------- 页眉页脚 ----------
\pagestyle{fancy}
\fancyhf{}
\fancyhead[C]{\small 基于 Next.js 的在线问卷系统设计与实现}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}

% ---------- 图表标题格式 ----------
\captionsetup{font=small, labelsep=space}
\renewcommand{\thefigure}{\arabic{section}-\arabic{figure}}
\renewcommand{\thetable}{\arabic{section}-\arabic{table}}
\renewcommand{\theequation}{\arabic{section}-\arabic{equation}}

% ---------- 章节编号深度 ----------
\setcounter{secnumdepth}{3}
\setcounter{tocdepth}{3}

% ---------- 章节标题格式（一级标题用中文数字） ----------
\ctexset{
  section = {
    name = {第,章},
    number = \chinese{section},
    format = \heiti \zihao{-2} \centering
  },
  subsection = {
    format = \heiti \zihao{-3} \raggedright
  },
  subsubsection = {
    format = \heiti \zihao{4} \raggedright
  }
}

% ---------- 排版微调 ----------
\emergencystretch=3em  % 缓解长 URL 等导致的 Overfull \hbox

% ---------- 自定义命令 ----------
\newcommand{\figref}[1]{图~\ref{#1}}
\newcommand{\tabref}[1]{表~\ref{#1}}
\newcommand{\eqreff}[1]{式~(\ref{#1})}

% ---------- 封面信息 ----------
\title{\textbf{基于 Next.js 的在线问卷系统\\设计与实现}}
\author{唐诗轶}
\date{\today}

% ============================================
% 正文开始
% ============================================
\begin{document}

% ---------- 封面 ----------
\input{chapters/cover}

% ---------- 摘要 ----------
\input{chapters/abstract}

% ---------- 目录 ----------
\tableofcontents
\newpage

% ---------- 正文 ----------
\onehalfspacing  % 1.5倍行距

\input{chapters/chapter1}
\newpage
\input{chapters/chapter2}
\newpage
\input{chapters/chapter3}
\newpage
\input{chapters/chapter4}
\newpage
\input{chapters/chapter5}
\newpage
\input{chapters/chapter6}

% ---------- 参考文献 ----------
\input{chapters/references}

% ---------- 致谢 ----------
\input{chapters/acknowledgement}

% ---------- 附录 ----------
\appendix
\input{chapters/appendix}

\end{document}

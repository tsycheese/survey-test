# Playwright 可选专用数据库。未设置时只允许回退到 localhost/127.0.0.1 的 DATABASE_URL。
E2E_DATABASE_URL=postgresql://user:password@127.0.0.1:5432/survey_e2e

# E2E 服务使用固定端口，避免占用日常开发的 3000 端口。
E2E_BASE_URL=http://127.0.0.1:3310
